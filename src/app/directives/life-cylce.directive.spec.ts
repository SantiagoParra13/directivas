import { LifeCylceDirective } from './life-cylce.directive';

describe('LifeCylceDirective', () => {
  it('should create an instance', () => {
    const directive = new LifeCylceDirective();
    expect(directive).toBeTruthy();
  });
});
